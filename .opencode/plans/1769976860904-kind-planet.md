# Plan: publint Configuration Analysis

## Summary

**Your publint configuration matches the Vercel AI SDK pattern** - both have:
- publint installed as a root devDependency
- A turbo task defined in `turbo.json`
- No per-package `publint` scripts

In both cases, running `pnpm publint` via turborepo is effectively a **no-op** because turbo requires matching scripts in each package's `package.json`.

## Comparison with Vercel AI SDK

| Component | This Project | Vercel AI SDK |
|-----------|--------------|---------------|
| Root dependency | `publint@^0.3.17` | `publint@0.2.12` |
| Root script | `turbo run publint` | `turbo publint` |
| Turbo task | ✅ Configured | ✅ Configured |
| Per-package scripts | ❌ None | ❌ None |
| CI runs publint | ? | ❌ No |

**Conclusion:** Your configuration is consistent with Vercel AI SDK. In both repos, publint is set up but not actually executing on any packages.

## Changes Required

Add `"publint": "publint"` script to each publishable package.

### 1. `packages/acme-utils/package.json`

Add to scripts section:
```json
"publint": "publint"
```

### 2. `packages/acme-core/package.json`

Add to scripts section:
```json
"publint": "publint"
```

## Verification

```bash
pnpm build && pnpm publint
```

Expected: publint validates both `@acme/utils` and `@acme/core`, reporting any issues with `main`, `module`, `types`, `exports` fields.
